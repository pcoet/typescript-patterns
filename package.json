{
  "name": "typescript-patterns",
  "version": "1.0.0",
  "description": "Algorithms, data structures, and programming patterns.",
  "scripts": {
    "clean": "rm -rf dist",
    "compile": "tsc",
    "test": "jest",
    "lint": "eslint .",
    "build": "npm run clean && npm run test && npm run compile && npm run build-site && npm run lint",
    "build-site": "parcel build site/index.html --dist-dir dist/site",
    "serve": "parcel site/*.html"
  },
  "keywords": [],
  "author": "David Huntsperger",
  "license": "Apache 2.0",
  "devDependencies": {
    "@types/express": "^4.17.15",
    "@types/jest": "^27.5.0",
    "@typescript-eslint/eslint-plugin": "^5.22.0",
    "@typescript-eslint/parser": "^5.22.0",
    "eslint": "^8.14.0",
    "jest": "^28.1.0",
    "parcel": "^2.8.3",
    "ts-jest": "^28.0.1",
    "typescript": "^4.6.4"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
